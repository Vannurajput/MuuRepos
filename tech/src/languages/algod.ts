
import { LanguageSupport } from "../../types";

export const algod: LanguageSupport = {
    id: 'algod',
    name: 'Deep Analysis Report',
    isVisual: true, // This tells the app to use a custom viewer instead of the editor
};
