
import MoText from './MoText';
import { MoTextProps, MoTextState } from './types';

// Export the main component
export default MoText;

// Export all types for consumers
export * from './types';
export type { MoTextProps, MoTextState };
